#include <stdio.h>

/* 
 * MenOfPassion(A[], n) {
 *     i = ?n / 2?;
 *     return A[i]; # 코드1
 * }
 */

int main()
{
	printf("1\n");  // n에 따른 MemOfPassion 함수의 수행 횟수. n이 늘어나도 횟수 증가 안함 
	printf("0");  // 수행 횟수를 다항식으로 나타내었을 때, 최고차항의 차수. O(1)
	
	return 0;
}
